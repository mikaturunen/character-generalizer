/**
 * Generates a single random number.
 * @param {number} min Minimum number
 * @param {number} max Maximum number
 * @returns {number} Number between the given values.
 */
export declare function random(min: number, max: number): any;
